{
    "devDependencies": {
        "husky": "^9.1.7",
        "prettier": "^3.6.2"
    },
    "scripts": {
        "prepare": "husky",
        "format": "prettier --write \"**/*.{js,ts,json,md,yml,yaml}\""
    }
}
